const mongoose = require('./backend/node_modules/mongoose'); const User = require('./backend/models/User'); mongoose.connect('mongodb+srv://admin:admin@cluster0.oic687k.mongodb.net/test?retryWrites=true&w=majority').then(async () => { const users = await User.find({ role: 'Resident' }).limit(10); console.log('USERS:', users.map(u => ({ name: u.name, address: u.address, phase: u.phase, legacy_category: u.assigned_category }))); process.exit(); }).catch(console.error);
